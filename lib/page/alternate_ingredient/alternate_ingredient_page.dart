import 'package:flutter/material.dart';

class AlternateIngredientPage extends StatelessWidget {
  const AlternateIngredientPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
